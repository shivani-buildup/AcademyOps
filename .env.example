# Database Configuration
# To use PostgreSQL, set this to a postgres URL (e.g. postgresql://user:password@localhost/academyops)
# To fallback to SQLite, use sqlite:///./data/academyops.db
DATABASE_URL=sqlite:///./data/academyops.db
